## Agent skills

### Issue tracker

Issues live as GitHub issues on `mao0507/MonthlyTracker`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root (not yet created). See `docs/agents/domain.md`.
